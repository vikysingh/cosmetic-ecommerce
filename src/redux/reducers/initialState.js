const initialState = {
    filters: {
        brand: '',
        product: '',
        minPrice: 0,
        maxPrice: 100
    },
    products: [
        /*
        {
            id: product id,
            price: product price
        }
        */
    ]
}

export default initialState